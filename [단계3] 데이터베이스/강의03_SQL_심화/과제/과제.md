# 과제03 — 통계 쿼리 작성

## 목표

JOIN, GROUP BY, 집계 함수를 활용하여 쇼핑몰 데이터에서 의미있는 통계를 추출합니다.

---

## 기본 셋업

강의03 예제(`예제/advanced.sql`)의 테이블과 데이터를 그대로 사용하세요.  
또는 아래 명령으로 파일을 실행하세요.

```bash
sqlite3 shop.db < ../예제/advanced.sql
```

---

## 통계 쿼리 과제 (5가지)

### 쿼리 1. 카테고리별 매출 통계

각 카테고리의 **총 매출**, **판매 수량**, **평균 단가**를 계산하세요.  
**총 매출 내림차순**으로 정렬하세요.

출력 컬럼: `카테고리`, `총매출`, `판매수량`, `평균단가`

```sql
-- 여기에 작성하세요
```

**힌트:** `order_items`와 `products`를 JOIN, `GROUP BY p.category`

---

### 쿼리 2. 월별 주문 수 및 매출

연도와 월별로 **주문 건수**와 **총 매출**을 구하세요.  
`ordered_at` 컬럼에서 연/월을 추출해야 합니다.

출력 컬럼: `연월`, `주문건수`, `총매출`

```sql
-- 여기에 작성하세요
-- 힌트: strftime('%Y-%m', ordered_at) 으로 연월 추출
```

---

### 쿼리 3. 회원별 구매 순위

각 회원의 **총 구매액**과 **구매 상품 종류 수**를 계산하고,  
총 구매액 내림차순으로 정렬하세요.

출력 컬럼: `회원명`, `총구매액`, `구매상품종류수`

```sql
-- 여기에 작성하세요
```

---

### 쿼리 4. 베스트셀러 상품 TOP 5

**판매 수량** 기준으로 가장 많이 팔린 상품 상위 5개를 조회하세요.

출력 컬럼: `상품명`, `카테고리`, `총판매수량`, `총매출`

```sql
-- 여기에 작성하세요
```

---

### 쿼리 5. 주문이 없는 회원 목록

한 번도 주문하지 않은 회원을 조회하세요.  
**LEFT JOIN**을 활용하세요.

출력 컬럼: `회원명`, `이메일`

```sql
-- 여기에 작성하세요
```

---

## 제출 형식

`과제정답/advanced.sql` 파일에 작성하세요.  
각 쿼리 위에 **한국어 주석**으로 설명을 달아주세요.

---

## 평가 기준

- [ ] 5개 쿼리 모두 작성
- [ ] JOIN을 올바르게 사용
- [ ] GROUP BY + 집계 함수 사용
- [ ] HAVING으로 그룹 필터링 시도
- [ ] 오류 없이 실행 가능
- [ ] 한국어 주석 포함
