# 과제02 — 쇼핑몰 상품 DB 조회

## 목표

쇼핑몰 상품 데이터베이스를 직접 만들고, 다양한 조건으로 조회하는 SQL 쿼리를 작성합니다.

---

## 기본 셋업

먼저 아래 SQL로 테이블과 데이터를 준비하세요.

```sql
CREATE TABLE products (
    id       INTEGER PRIMARY KEY AUTOINCREMENT,
    name     TEXT    NOT NULL,
    price    INTEGER NOT NULL,
    category TEXT    NOT NULL,
    stock    INTEGER DEFAULT 0,
    rating   REAL    DEFAULT 0.0
);

INSERT INTO products (name, price, category, stock, rating) VALUES
    ('삼성 노트북',    1200000, '전자제품', 15, 4.5),
    ('애플 맥북',      2000000, '전자제품',  8, 4.8),
    ('LG 모니터',       450000, '전자제품', 20, 4.3),
    ('로지텍 마우스',    45000, '전자제품', 100, 4.1),
    ('기계식 키보드',   120000, '전자제품', 35, 4.6),
    ('나이키 운동화',   150000, '의류',      30, 4.4),
    ('아디다스 티셔츠',  45000, '의류',      80, 4.0),
    ('청바지',          89000, '의류',       60, 4.2),
    ('파이썬 입문서',   28000, '도서',       40, 4.7),
    ('자바스크립트 완벽 가이드', 45000, '도서', 25, 4.5),
    ('클린 코드',       32000, '도서',       55, 4.9),
    ('프라이팬',        35000, '주방',       70, 4.3),
    ('전기밥솥',       180000, '주방',       25, 4.6),
    ('믹서기',          55000, '주방',        0, 3.8);
```

---

## 쿼리 작성 과제 (5가지)

### 쿼리 1. 가격 범위 필터

**50,000원 이상 200,000원 이하** 상품을 **가격 오름차순**으로 조회하세요.  
출력 컬럼: `name`, `price`, `category`

```sql
-- 여기에 작성하세요
```

### 쿼리 2. 카테고리 필터

**전자제품** 카테고리 상품 중 **재고가 20개 이상**인 것만 조회하세요.  
출력 컬럼: `name`, `price`, `stock`

```sql
-- 여기에 작성하세요
```

### 쿼리 3. 품절 상품 조회

**재고(stock)가 0인** 품절 상품을 모두 조회하세요.  
출력 컬럼: `name`, `category`

```sql
-- 여기에 작성하세요
```

### 쿼리 4. 평점 정렬 + 제한

**평점 4.5 이상** 상품을 **평점 내림차순**으로 정렬하여 **상위 3개**만 조회하세요.  
출력 컬럼: `name`, `rating`, `price`

```sql
-- 여기에 작성하세요
```

### 쿼리 5. 이름 검색

상품 이름에 **'스'** 가 들어가는 상품을 모두 조회하세요.  
출력 컬럼: `name`, `price`, `category`

```sql
-- 여기에 작성하세요
```

---

## 제출 형식

`과제정답/queries.sql` 파일에 5개의 쿼리를 모두 작성하고, 각 쿼리 위에 설명 주석을 달아주세요.

---

## 평가 기준

- [ ] 5개 쿼리 모두 작성
- [ ] 조건(WHERE), 정렬(ORDER BY), 제한(LIMIT)을 올바르게 사용
- [ ] 실행 시 오류 없이 동작
- [ ] 각 쿼리에 한국어 주석 포함
